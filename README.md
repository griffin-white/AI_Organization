# Files
# 1. app.py
To run this download app.py and the requirements.txt to your device and then install streamlit unless you already have it installed.
 Just run "pip install streamlit" in the powershell.
 Then to run the Program type "python -m streamlit run app.py".
 Finally quick sanity check make sure your in the right directory on powershell lol

